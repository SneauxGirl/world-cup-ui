type Props = {
  className?: string;
};

/** LinkedIn mark from `public/icons/linkedin.svg` (Streamline Phosphor). */
export function LinkedInLogoIcon({ className }: Props) {
  return <span className={className} aria-hidden />;
}
